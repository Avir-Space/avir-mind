# AVIR Mind — environment variables
# Copy to .env.local and fill in. Never commit .env.local.

# Supabase project (client-safe)
NEXT_PUBLIC_SUPABASE_URL="https://jktzwgaqnyjzhbmryqya.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-or-publishable-key"

# Supabase service role — server-only, NEVER expose to the browser.
# Not required by the app in Phase 0 (seeding runs inside Postgres via trigger).
# SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Public site URL — used for auth email redirects (magic links, password reset)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
