import { test } from "@playwright/test";

/**
 * Module 8 — MRO, Public API & AVIR Index
 *
 * SCAFFOLDED — not yet implemented. Follows the structural pattern of
 * modules/01-foundation-auth.spec.ts (signInAs → assert → cleanup). Each
 * test.fixme documents an intended case for a later pass.
 */
test.describe("08 · MRO / API / Index", () => {
  test.fixme("8.1 Tenant switch operator↔MRO; MRO nav + shop floor", async () => {});
  test.fixme("8.2 Work package findings → notify customer flow", async () => {});
  test.fixme("8.3 Public API: key create → GET /v1/signals → 429 rate limit", async () => {});
  test.fixme("8.4 AVIR Index preview gated; activation flow (hash + step-up)", async () => {});
  test.fixme("8.5 Public /avir-index 404 until visible; embed widget renders", async () => {});
});
